<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.midterm.foodSNS.result.mapper.IResultMapper">


	<select id="recommand" resultType="recipe">	
	SELECT * FROM cookDB WHERE weather= #{weather} and condition2 = #{condition2} and feeling = #{feeling}	
	</select>

	<select id="recipeDetail" resultType="recipe">
		SELECT * FROM cookDB
		WHERE cooknum = #{cooknum}
	</select>
	
	<select id="getLike" resultType="like">
		SELECT * FROM likeDB
		WHERE cooknum = #{cooknum}
	</select>
	
	<select id="chkLike" resultType="like">
		SELECT COUNT(*) FROM likeDB
		WHERE cooknum = #{cooknum} AND JSON_SEARCH(user_ids, 'one', #{userIds}) IS NOT NULL;
	</select>
	
	<update id="updateLike">
		UPDATE likeDB
		SET likenum = #{likenum}, user_ids = JSON_ARRAY_APPEND(user_ids, '$', #{userIds})
		WHERE cooknum = #{cooknum}
	</update>











</mapper>