<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.midterm.foodSNS.reply.mapper.IReplyMapper">

	<insert id="replyRegist">
		INSERT INTO reply
		(cooknum, reply, user_id, user_nick)
		VALUES(#{cooknum}, #{reply}, #{userId}, #{userNick})
	</insert>
	
	<select id="getList" resultType="reply">
		SELECT * FROM reply
		WHERE cooknum = #{cooknum}
		ORDER BY rno DESC
		LIMIT #{paging.pageStart}, #{paging.cpp}
	</select>
	
	<select id="getTotal" resultType="int">
		SELECT COUNT(*)
		FROM reply
		WHERE cooknum = #{cooknum}
	</select>
	
	<update id="update">
		UPDATE reply
		SET reply = #{reply}, update_date = current_timestamp
		WHERE rno = #{rno}
	</update>
	
	<delete id="delete">
		DELETE FROM reply
		WHERE rno = #{rno}
	</delete>


</mapper>